# Определение процесса
- **Процесс** - совокупность машинных команд и данных, которая обрабатывается в рамках вычислительной системы и обладает правами на владение некоторым набором _ресурсов_.

- Ресурсы могут принадлежать только одному процессу, либо ресурсы могут разделяться между процессами — **разделяемые ресурсы**.
- Разделяемым ресурсом может быть область оперативной памяти.

- Выделение ресурсов процессу
	- Предварительная декларация - до начала выполнения
	- Динамическое пополнение списка ресурсов
		- Модель, при которой ресурс выделяется по запросу
			- Примеры: использование времени процессора в ОС
			- Виртуальная оперативная память

- Количество допустимых процессов в системе - *ресурс ВС*

# Жизненный цикл процессора
- Основной из задач ОС является поддержание жизненного цикла процесса.
	- Совокупность состояний, в которых можно находиться процесс. Оно декларируется в системе
	- Формирование процесса
	- Исполнение процесса
	- Ожидание процесса
	- Завершение процесса

# Модельная организация ОС
- Предположим, что в нашей модельной ОС все процессы находятся либо в буфере ввода процессора (БВП) (некоторая область на внешнем запоминающем устройстве, в которой размещается информация о процессах и процессы таам находятся до момента начала выполнения), либо буфер обрабатываемых процессов (БОП) (предполагаем, что размещаются процессы, которые находятся в обработке, они там находятся до завершения). После завершения процесса, ОС может передать процесс из БВП в БОП

## Модель пакетной однопроцессной системы
0. Поступление процесса в очередь на начало обработки ЦП (процесс попадает в БВП)
1. Начало обработки процесса на ЦП (из БВП в БОП)
2. Завершение выполнения процесса, освобождение системных ресурсов
![](_resources/Pasted%20image%2020251009110351.png)
## Модель пакетной мультипроцессорной системы
![](_resources/Pasted%20image%2020251009110421.png)
## Модель ОС с разделением времени
![](_resources/Pasted%20image%2020251009111003.png)
- Появляется переход из исполнения в очередь на выполнения

- Модификация:
![](_resources/Pasted%20image%2020251009111158.png)
- Процессор выгружается во внешнюю память

# Типы процессов
- "**Полновесные**" процессы - процессы, выполняющиеся внутри защищённых участков оперативной памяти
- "**Легковесные**" процессы (нити) или сопрограммы - активируются полновесным процессором на принадлежащие ему оперативной памяти. Они не защищены друг от друга. Могут быть несколько нитей.
	![](_resources/Pasted%20image%2020251009111849.png)

# Контекст процесса
- **Пользовательская составляющая** — это текущее состояние программы (т.е. совокупность машинных команд и данных, размещённых в ОЗУ и характеризующих выполнение данного процесса);
- **Аппаратная составляющая** – отражает актуальное состояние центрального процессора в момент выполнения данного процесса (т.е. это актуальное состояние регистров, настроек процессора и т.д.);
- **Системная составляющая** – это структуры данных операционной системы, содержащие характеристики процесса:
	- содержат информацию идентификационного характера (PID процесса, PID «родителя» и т.д.); информацию о содержимом регистров (РОН, индексные регистры, флаги и т.д.);
	- o информация о выделенных процессу ресурсах, настройках и использовании виртуальной и физической оперативной памяти;
	- o информацию, необходимую для управления процессом (состояние процесса, приоритет и т.д.). Отметим, что системная составляющая процесса содержит копию аппаратно-системной составляющей, если процесс приостановлен: к примеру, в случае, если используется виртуальная страничная память, то в контексте разиещается «расщиренная» копия таблицы страниц в которой размещается вся информация о кажой витруальной странице процесса (номер физической страницы, если она загружена в память; расположение страницы на внешнем устройстве, если такая страница есть и не загружена в память; признак отсутствия страницы).

- Реальный идентификатор определяет права процесса

# Первое определение процесса в Unix
- Процессоров в Unix считается:
	- Объект, зарегистрированный в таблице процессов ОС
	- Объект, порождённый системным вызовом fork()

- **Процесс в Unix** - объект, зарегистрированный в таблице процессов в Unix
![](_resources/Pasted%20image%2020251009113225.png)
- Размер таблицы является параметром найстройки ОС
- Таблица процессов обеспечивает уникальное именование процессов
- Номер записи процесса в таблице называется **идентификатором процесса** (PID - Process Identifier)
- Каждая запсь таблицы процессов имеет ссылку на контекст процесса, который структурно состоит из пользовательской, системной и аппартной составляющих
## Пользовательская (программная) составляющая 
- Тело процесса
- Состоит из двух частей: сегмент кода и сегмент данных

### Сегмент кода
- Машинные команды и неизменяемые константы
- То есть не изменяемая *программным* способом часть тела процесса (**Они могут меняться только благодаря системному вызову**)

#### Разделение сегмента кода
- Возможность для предназначена для оптимизации использования ОП
- Каждый обрабатываемый в системе процесс текстового редактора в пользовательской

составляющей хранит ссылку на единственную копию сегмента кода редактора, а сегмент

данных у каждого из процессов свой
### Сегмент данных
- Область статической памяти процесса, область разделяемой памяти (область памяти, которая может принадлежать двум и более процессам одновременно)

## Аппаратная составляющая 
- Регистры
- Аппаратные таблицы процесса
- Они характеризуют актуальное состояние процесса в момент его выполнения на процессоре
- Конкретная структура аппаратной составляющей зависит от конкретного процессора; обычно она включает счётчик команд, регистр состояния процессора, аппарат виртуальной памяти, регистры общего назначения и т.д.

## Системная составляющая
- идентификатор родительского процесса;
- текущее состояние процесса;
- приоритет процесса;
- реальный идентификатор пользователя-владельца (идентификатор пользователя, сформировавшего процесс);
- эффективный идентификатор пользователя-владельца (идентификатор пользователя, по которому определяются права доступа процесса к файловой системе);
- реальный идентификатор группы, к которой принадлежит владелец (идентификатор группы, к которой принадлежит пользователь, сформировавший процесс);
- эффективный идентификатор группы, к которой принадлежит владелец (идентификатор группы «эффективного» пользователя, по которому определяются права доступа процесса к файловой системе);
- список областей памяти;
- таблица дескрипторов открытых файлов процесса (именно дескрипторов, т.к. один и тот же файл может быть открыт в системе многократно); файловый дескриптор – это системная структура данных, отражающая актуальное состояние работы процесса с файлом;
- информация о том, какая реакция установлена на тот или иной сигнал (аппарат сигналов позволяет передавать воздействия от ядра системы процессу и от процесса к процессу);
- информация о сигналах, ожидающих доставки в данный процесс;
- сохраненные значения аппаратной составляющей (когда выполнение процесса приостановлено).

# Второе определение процесса в Unix
- **Процесс в UNIX** — это объект, порожденный системным вызовом **fork()**
- **Системный вызов** — обращение процесса к ядру ОС за выполнением тех или иных действий.
```c
#include <sys/types.h>
#include <unistd.h>

pid_t fork ( void );
```
- При удачном завершении возвращается:
	- сыновнему процессу значение 0
	- родительскому процессу PID порожденного процесса

- При неудачном завершении возвращается –1, код ошибки устанавливается в переменной **errno**
	- <errno.h>

- Заносится новая запись в таблицу процессов
- Новый процесс получает уникальный идентификатор
- Создание контекста для нового (сыновьего) процесса

## Создание нового процесса
- **Составляющие контекста, наследуемые при вызове fork()**:
	- Окружение
	- Файлы, открытые в процессе-отце
	- Способы обработки сигналов
	- Разрешение переустановки эффективного идентификатора пользователя
	- Разделяемые ресурсы процесса-отца
	- Текущий рабочий каталог и домашний каталоги

	 - Идентификатор процесса (PID)
	- Идентификатор родительского процесса (PPID)
	- Сигналы, ждущие доставки в родительский процесс
	- Время посылки ожидающего сигнала, установленное системным вызовом **alarm()**
	- Блокировки файлов, установленные родительским процессом

## Схема создания нового процесса
![](_resources/Pasted%20image%2020251009115807.png)
**TODO** разобраться c кодом

# Нити в Unix
Легковесные процессы Unix или нити (далее **нити**) являются ресурсом, локализировнным в рамках процесса (полновесного процесса). Т.е. они создаются и могут взаимодействовать между собой только в рамках процесса, в котором они созданы. Нити, созданные в разных процессах друг друга «не видят» и, в общем случае, не могут осуществлять взаимодействие. Нити, созданные «внутри» процесса, разделяют глобальные переменные и системные ресуры, выделенные процессу. Стек и регистры (включая счетчик команд) локализованы в рамкак каждой нити.

## Создание нити

`int pthread_create(pthread_t *tid, const pthread_attr_t *attr, void* (*start)(void *), void *arg)`

#### `pthread_t *tid`

- **Что это:** Указатель на переменную типа `pthread_t`.
- **За что отвечает:** В эту переменная функция запишет **идентификатор (ID)** вновь созданного потока. Этот идентификатор подобен "имени" потока и используется в других функциях (например, `pthread_join`, `pthread_cancel`) для управления именно этим потоком.
- **Аналогия:** Как `pid_t` для процессов, но для потоков внутри одного процесса.

####  `const pthread_attr_t *attr`

- **Что это:** Указатель на структуру `pthread_attr_t`, которая описывает **атрибуты** создаваемого потока.
- **За что отвечает:** Определяет свойства потока: размер стека, приоритет, состояние (присоединяемый или отсоединенный) и другие. Если передать `NULL`, поток будет создан с атрибутами **по умолчанию**, что подходит для большинства случаев.
- **Примеры атрибутов:**
    - **Detached state:** Поток, созданный как "detached", освобождает свои ресурсы сразу после завершения, и к нему нельзя присоединиться через `pthread_join`.
    - **Stack size:** Можно задать конкретный размер стека для потока.

#### `void* (*start_routine)(void *)`

- **Что это:** Указатель на функцию, которая будет выполнена в новом потоке.
- **За что отвечает:** Это **точка входа** для нового потока. Как только поток создан, он начинает выполнение именно с этой функции.
- **Сигнатура функции:**
    - Функция должна принимать один аргумент типа `void*` (указатель на что угодно).
    - Функция должна возвращать значение типа `void*` (указатель на что угодно).
    
####  `void *argv

- **Что это:** Указатель `void*` на аргумент, который будет передан в функцию `start_routine`.
- **За что отвечает:** Позволяет передать данные из основного потока в новосозданный. Через этот аргумент можно передать адрес любой структуры данных, что делает запуск потока очень гибким.
- **Важно:** Указатель `arg` просто копируется в новый поток. Он должен оставаться валидным на момент начала выполнения потока. Нельзя передавать указатель на локальную переменную, которая может выйти из области видимости (уничтожиться) раньше, чем поток начнет ее использовать.
## Читатели и писатели (задача резервирования ресурсов)

### Вариант с ошибкой
```c
/* семафор для доступа в критическую секцию - контроль за
доступом к «rc» (разделямый ресурс) */
semaphore mutex = 1;
/* семафор для доступа к базе данных */
semaphore db = 1;
/* количество читателей внутри хранилища */
int rc = 0
```

```c
/* процесс-читатель */
void Reader(void)
{
	down(&mutex); /* получить эксклюзивный доступ к «rc»*/
	
	rc = rc + 1; /* еще одним читателем больше */
	
	/* если это первый читатель, нужно заблокировать
	эксклюзивный доступ к базе */
	if(rc == 1){
		down(&db);
	}

	up(&mutex); /*освободить ресурс rc */
	ReadDataBase(); /* доступ к данным */
	down(&mutex); /*получить эксклюзивный доступ к «rc»*/
	rc = rc – 1; /* теперь одним читателем меньше */
	/*если это был последний читатель, разблокировать
	эксклюзивный доступ к базе данных */
	if(rc == 0)
	up(&db);
	up(&mutex); /*освободить разделяемый ресурс rc*/
	UseDataRead(); /* некритическая секция */
}
```

```c
void writer ()
{ 
	think_up_data () ;
	down(&db) ;
	write_data_base () ;
	up(&db) ;
}
```

**TO DO ПЕРЕПИСАТЬ**

**TO DO: разобраться, что такое семафор**
### Уточнение решения
![](_resources/Pasted%20image%2020251030090617.png)
- `r_block` - флажок, который говорит о том, что первый читатель вошёл в систему и заблокирован на mutex 
- `wc` - количество писателей до первого читателя

Когда появился первый читатель, читатель получил приоритет над писателями

